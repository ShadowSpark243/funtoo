import 'package:flutter/material.dart';

class NewGroupChatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Group Chat'),
      ),
      body: Center(
        child: Text('Create a new group chat here.'),
      ),
    );
  }
}
